Image Processing Unit 2 Objective The goal of this assignment is to implement three common image processing filters (Gaussian Filter, Sobel Filter, and Median Filter) and analyze their performance using different computational approaches: 1. Pure Python (No external optimizations) 2. NumPy (Vectorized operations for optimization) 3. NumPy + Cython (Further optimization using compiled code)  Problem Statement You will apply the following filters to a grayscale image: 1. Gaussian Filter (Blurring/Noise Reduction) o This filter smooths the image by applying a weighted average of neighboring pixels using a Gaussian kernel. o The standard 3×3 Gaussian kernel is: 𝐺=116%121242121( o You will convolve this kernel over the image to produce a blurred version of the original. 2. Sobel Filter (Edge Detection) o This filter is used to detect edges by computing the gradient of the image in both the X and Y directions. o The standard Sobel kernels are: § X-Direction 𝑆!=%−101−202−101(  
§ Y-Direction  𝑆!=%−1−2−1000121(  o Compute the gradient magnitude: 𝐺=	-𝑆!"+𝑆#" o This will highlight the edges of objects in the image. 3. Median Filter (Noise Reduction) o This is a non-linear filter that replaces each pixel with the median value of its neighboring pixels. o This method is highly effective in removing salt-and-pepper noise while preserving edges. o Example: For a 3×3 neighborhood: [10, 50, 200] [30, 25, 70] [20, 80, 90] The median value (50) would replace the center pixel. Tasks 1. Load an Image o Convert it to grayscale. o Display the original image before applying filters. 2. Implement the Filters o Implement each filter using: § Pure Python (without NumPy) § NumPy (using matrix operations for optimization) § NumPy + Cython (optimizing with compiled C functions) 3. Compare Performance o Measure execution time for each implementation. o Compare the performance of Pure Python vs. NumPy vs. NumPy + Cython. o Create a table summarizing execution times. 4. Visualize and Analyze Results o Display the filtered images for each method. 
o Discuss the trade-offs between execution time and ease of implementation. Expected Deliverables 1. Code Implementation: o Well-structured Python scripts for each filter (Pure Python, NumPy, NumPy + Cython). o Clearly labeled functions for each filter. 2. Performance Analysis: o A report (PDF format) comparing execution times. o Insights into how computational optimization affects performance. 3. Visual Results: o Original vs. Filtered images for each filter. o Comparative visualization showing the impact of each filter. 4. Repository: o A well-documented GitHub repository containing all code, report, and instructions. Evaluation Criteria (100 points total) Criteria Points Correct implementation of the filters (Python, NumPy, Cython) 40 pts Performance comparison and timing analysis 20 pts Quality of visualizations and output images 20 pts Clarity of code, structure, and comments (Repository) 10 pts Discussion and insights on performance differences 10 pts  Submission Guidelines • Submit a PDF Report containing: o Problem description o Implementation details o Performance analysis o Visual results • Submit a GitHub repository containing: o Python scripts for each filter o Cython implementation o requirements.txt for dependencies o A README.md file with setup instructions 